[package]
name = "runner"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "bench-cache"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
object_store = { version = "0.11", features = ["gcp"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
